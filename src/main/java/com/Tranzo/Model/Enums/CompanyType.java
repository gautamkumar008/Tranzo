package com.Tranzo.Model.Enums;

public enum CompanyType {
    DAILY,              // Daily scheduled service
    WEEKLY,             // Weekly routes
    ON_DEMAND,          // On request
    CONTRACT,           // Fixed long-term contract
    PER_TRIP,           // Pay per shipment
    SUBSCRIPTION,       // Monthly subscription model
    DEDICATED,          // Dedicated vehicle/service
    SHARED,             // Shared logistics (cost-saving)
    HYBRID              // Mix of multiple types
}
